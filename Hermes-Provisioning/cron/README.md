# Cron jobs

Empty so far — no `hermes cron create` has actually been run yet. When one
is, its declaration (schedule, name, prompt or --skill, whether --no-agent)
goes here as its own file, matching the pattern in `mcp-servers/`.
