# Skills

Empty so far — no Skill has been authored yet. When one is, it goes here
matching Hermes' own real structure exactly
(`skills/<category>/<skill-name>/SKILL.md` + optional `scripts/`), so it
can be copied directly into a real Hermes install's own `skills/`
directory. See `Implementation/Plans/2026-08-20-backend-architecture-
redesign.md` ("Hermes Skills, real shape") for the real, researched
SKILL.md format.
