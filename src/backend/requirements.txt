fastapi>=0.115
uvicorn[standard]>=0.32
pytest>=8.3
httpx>=0.27
pydantic-settings>=2.5
pywin32>=306; sys_platform == "win32"
apscheduler>=3.10
langgraph>=1,<2
langchain-openai
# `mcp` and `langchain-mcp-adapters` were dropped on 2026-09-04 with the MCP
# Tool layer. Their only importer was app/data_access/system/tools/registry.py
# (`from mcp.server.fastmcp import FastMCP`), which is deleted -- so the
# `mcp<2` pin that fresh installs needed is retired along with the reason for
# it. Nothing in src/backend imports either package now; re-add them only if
# Second Brain ever becomes an MCP server again, which is not the design.
anthropic
pypdf
python-multipart
pyyaml
websockets>=13
