"""Agent entity -- a real Hermes profile, composed with Second Brain's
own Section/type/visual overlay. See agent.py for the shape,
agent_manager.py for the AgentManager (get_all/get_by_id wired; create/
update and the real SOUL.md composer are separate, not-yet-built
follow-up work). Pipelines are a separate concept, not yet folded in
here."""
