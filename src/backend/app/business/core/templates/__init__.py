"""Template entity -- defines what a vault Note's shape is (note naming
rule, per-section write access, frontmatter defaults). See template.py
for the shape, template_manager.py for the TemplateManager (methods
not yet wired -- scaffolding only)."""
