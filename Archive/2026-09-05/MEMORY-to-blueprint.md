# Blueprint source material

The 51 entries behind `Documentation/Framework/`. Pass 3 distilled the gaps these covered into `Vault-Layout.md`, `Hermes-Runtime.md` and the Section model in `Artifacts.md`; the rest were already covered by the existing four pages. Kept here as provenance for every claim in those docs.

Verbatim from `MEMORY.md` as it stood on 2026-09-05. Nothing edited.

---

## Entry 000  (Decisions section, 3855 chars)

- **[2026-08-22] Vault-write capability is being split off the Primary (WhatsApp) Hermes profile into separate "specialist" profiles, one per domain — first instance `opp-manager` (owns the `track-opportunities` Skill).** Reason: the operator's own concrete worry ("might destroy the vault by Accident") plus a real finding that a prompt-only "you never write" instruction is weaker than actually removing the write-capable Skill from the Primary profile's own `skills/` folder (a prompt can be deviated from; a missing capability can't). Mechanism: `hermes profile create <name> --clone` (own HERMES_HOME, own SOUL.md, own copy of skills), then move the relevant Skill folder out of the Primary profile's `skills/` tree (e.g. into a sibling `_disabled-skills-on-primary/`, not deleted). Primary still runs the existing mechanical cron pipelines for now (email/meeting capture, thread/file summarization, company tagging) — migrating those off Primary too is a separate, not-yet-scheduled follow-up. Second instance, 2026-08-22: `notes-manager` (owns `capture-notes`) — the catch-all Primary relays to when a message is content to log rather than a request any specialist owns (a voice note, a passing observation) — instead of the older "tell Mahmoud I don't have this skill" fallback. Third instance, 2026-08-22: `files-manager` (owns `capture-files`) — the file counterpart, for an uploaded file with no stated context. Unlike the other two specialists, this one is NOT trimmed to a single Skill — it also keeps the bundled `docx`/`pptx`("powerpoint")/`xlsx`/`pdf`/`nano-pdf`/`ocr-and-documents` extraction Skills, since real summarization needs them (reasoning_effort left at `medium` here, not lowered to `low` like the other two specialists, for the same reason). Fourth instance, 2026-08-23: `azure-calculator` — operator: "I want to Create an Agent to be my Azure Calculator Helper... it need to be simple and very smart, Ask the right Questions to get based on the Solution." Different shape from the first three: not a vault-writer at all (it writes nothing), a genuinely conversational specialist reached directly (not via Primary's one-shot relay, unlike `opp-manager` — it needs a real back-and-forth to nail down what's being priced before calculating, which a one-shot relay call can't support). Owns one new Skill, `pricing/azure-cost-calculator` (`lookup_azure_price.py`, real, verified-live queries against Microsoft's own public Azure Retail Prices API, `prices.azure.com` — no API key needed). `reasoning_effort` left at `medium` (cloned default), not lowered — real judgment is needed to map a vague workload description onto the right Azure service/region/SKU and to pick between several real matching price rows (pay-as-you-go vs. Spot, Windows vs. Linux, etc.), closer to `files-manager`'s own reasoning needs than the two simpler capture specialists'. Fifth instance, 2026-08-24: `daily-briefing` — operator: "let's bring it back smarter instead of just listing emails and meetings," authorized to build fully autonomously overnight ("I will go to sleep run this part autonomous"). Different shape again: purely READ-ONLY (writes nothing, takes no action at all — deliberately sidesteps the still-undecided Approval question, operator: "I don't know how to handle this part yet," rather than guessing at a flow), owns `day-planning/daily-briefing` (`my_day_lookup.py` + `note_detail.py`, both calling Second Brain's own live `/my-day/*`/`/vault-search/notes/{stem}` REST API rather than reimplementing its classification logic — same "call the real API" pattern `azure-cost-calculator` established). Built on top of a same-night prerequisite bug fix to `my_day.py` itself (see the `list_notes_in_kind_folder` non-recursive-glob / stale `Work/Emails/` entry below) — this agent would have had nothing real to reason over otherwise.

## Entry 001  (Decisions section, 1439 chars)

- **[2026-08-22] `capture-files`' own structure corrected to folder-per-file (not folder-per-day) after real use: `Work/Files/<date>/<filename stem>/<real file> + <filename stem>.md`.** Mirrors this vault's own established Thread/Meeting/Opportunity pattern (a folder named after the thing, holding a same-named `.md` alongside real content) — operator's own correction, 2026-08-22, after the flat per-day layout shipped first. Also fixed in the same pass: the script now accepts an optional `filename` override — WhatsApp's local download cache renames incoming media (observed live: `doc_e9675ce5da81_<real name>`), and the agent must pass the REAL original filename (from message/media metadata) rather than trusting the download path's own name. `add_file_detail()` (CLI: `--append`) is a second job on the same script — appends a `## Details` section to an already-captured file's own description note for a deeper follow-up pass; this is the ONLY place further analysis output goes (never a new file, never sent to the operator directly). Extended same day to accept an optional `images` list — when the deeper pass finds a real diagram/architecture visual worth seeing (not just describing), the agent renders that specific slide/page to a PNG, and the script copies it into the file's own folder and embeds it (`![[filename]]` + caption) right in the Details section. Operator's own framing: worth seeing, not just prose-described.

## Entry 006  (Decisions section, 2952 chars)

- **[2026-08-25] `REQ-SB-80`'s RegistryLoader lives at `app/data_access/registry/` and its real data at `<vault>/.second-brain/data/` — a real folder tree, not another flat JSON file — and is a genuine, conscious reversal of `ADR-003`'s "Second Brain never owns Hermes data" stance.** Reason: every agent added this session required a hand-edit to `agents_map_adapter.py`'s hardcoded dicts; the fix is Second Brain's backend becoming the real owner of Sections/Agents/Tools/Skills/Providers, with Hermes as a regenerable push target (push mechanism itself still undesigned — see `REQ-SB-80`). Boot is staged and fail-loud (operator: "Fail Loud so I can fix or remove") — the first invalid file anywhere under `data/` halts that boot attempt with the exact file + reason, previously-loaded data is left untouched. Hot-reload reuses the exact same `boot()` coroutine on a 2-second poll of the tree's own (path, mtime) fingerprint — no new file-watcher dependency, no second validation path (operator: "think consalidation, how can we reuse"). `GET /boot-status` / `POST /boot-status/retry` (`app/api/boot_router.py`) are pollable from the instant the app accepts connections (the loader runs as a fire-and-forget task after `yield`, same pattern as `vault_indexing.rebuild_index`), which is what lets the frontend's `BootGate` show real per-stage progress instead of a blank screen. **[2026-08-25 follow-up] `agents_map_adapter.py`'s Type/Section/depends_on/is_background_agent/display_name are now Registry-backed** — the 5 hardcoded dicts this file used to hand-edit per agent are deleted; `_registry_agent(agent_id)` reads `registry_loader.get_registry()` instead, falling through to the exact same defaults the dicts always used (`worker`/Data Gatherer/no deps/not background/raw Hermes name) for an agent not yet migrated. **This broke `migrate_to_data_layer.py` in a real, non-obvious way, worth remembering**: the script runs as its own standalone process, so `registry_loader.get_registry()` is `None` there unless the script calls `registry_loader.boot()` itself first — without that, every adapter call it makes would see an empty Registry and silently overwrite every already-correct real placement on disk with bare defaults on a re-run. Fixed by having the script `asyncio.run(registry_loader.boot())` before calling any adapter function, and refusing to proceed if that boot comes back `failed`. Generalizes: any future script/process that calls into `agents_map_adapter.py`'s Registry-backed helpers must boot its own Registry first — `get_registry()` is per-process in-memory state, not a shared/persisted read. Verified live: re-ran the migration script after this fix, confirmed on-disk `Agent-config.json` files were byte-identical (genuinely idempotent), then confirmed the live Agents Map (6 sections, 32 agents, correct friendly names/types/section placement/dependency edges) renders unchanged through the new Registry-backed path.

## Entry 012  (Decisions section, 829 chars)

- **[2026-08-22] A cron job's real per-run "what happened" is linkable two ways off the SAME `executions.db` row, no guessing involved: its per-run markdown report is filed under that run's own `finished_at` timestamp (`cron/output/<job_id>/<finished_at as YYYY-MM-DD_HH-MM-SS>.md`), and its raw `agent.log` lines are tagged with a session id built from that run's own `started_at` (`[cron_<job_id>_<started_at as YYYYMMDD_HHMMSS>]`).** Confirmed live by matching a real execution row's own timestamps against both real files before writing any code. `hermes_cron.py` reads all three real sources (`cron/jobs.json`, `cron/executions.db` via read-only sqlite3, and these two timestamp-derived paths) directly — no gateway/CLI dependency, same "not ours to own, read the real files" convention as `hermes_definitions.py` (ADR-001).

## Entry 013  (Decisions section, 1016 chars)

- **[2026-08-22] `Work/Notes/<YYYY-MM-DD>/<Auto-Generated Title>.md` (folder per day, one file per note) is the quick-capture inbox — deliberately NOT filed under any Customer/Partner hub.** Operator's own explicit choice: "General Notes will Stay General for now... Notes Can be Classified by Day [...] we will work on it later" — a future reasoning pass re-classifies/re-files these; `capture-notes`'s only job is fast, lossless, no-questions-asked capture. Split of responsibility: the calling agent (`notes-manager`) generates the Title and Summary itself (it already read the content) and passes the Body through verbatim, unedited; the script is purely mechanical (slugify title → filename, same-day collision handling, best-effort `[[wikilink]]` of any real Customer/Partner name it recognizes in summary/body, matched by exact hub-note name — never blocking, never a resolution gate, unlike `track-opportunities`'s strict Customer requirement). Note structure: frontmatter, then `## Summary`, then `## Body`.

## Entry 018  (Decisions section, 2934 chars)

- **[2026-08-27] Second Brain's OWN backend now carries its own copy of `vault_manager.py` (`app/data_access/vault_manager.py`) and uses it directly for a real feature (Cockpit Documents upload) — the first time the backend itself, not a Hermes Skill, has been a `vault_manager.py` consumer.** Built for the Cockpit Documents tab (operator, 2026-08-27: "I will need to upload a file or a Screenshot while I am in the meeting and I will need this to be Stored in the meeting"). First attempt was a bespoke hand-rolled writer (`documents.py` reimplementing its own slugify/collision/frontmatter logic) — operator correction, sharp and direct: *"documents.py? We have vault writer for this"* and *"We need to stop this we are trying to build a Standard and you deviate from it everytime... we built a full Architecture to avoid creating a new file everytime."* Real design fork surfaced and resolved with the operator: `vault_writer.py`'s own existing `write_attachments` was NOT the right reuse target either — it's a different, incompatible convention (nests by message-segment under a separate `attachments/` tree, built for the OLD email-capture pipeline where one Thread accumulates attachments from many incoming messages over time); reusing it here would have scattered uploads into a second tree instead of the folder already open in Obsidian. Resolved: reuse `vault_manager.py` itself (copied into the backend, matching its own "prepare here, apply where it's needed" convention, applied to Second Brain's backend as a normal consumer for the first time) with the SAME real `file` Template `capture-files` already uses (`note_own_folder`, Summary/Details sections) — operator's own explicit shape: *"Do Files Folder and again include a folder per file and Md file that contains a Summary, in the meetings."* The one real difference from `capture-files`' own top-level `Work/Files/` placement: `note_name` is computed from the SUBJECT note's own real relative path (via `vault_indexing`, never reconstructed/guessed) plus `/Files`, so an upload lands at `Work/Meetings/<date>-<Subject>/Files/<date>-<upload stem>/` — inside the meeting's own already-existing folder, not a separate tree. A companion `.md`'s `Summary` section holds an optional caption if given, else an honest "no caption given" placeholder — never a fabricated content-based summary (no text-extraction/LLM call in this path; a screenshot has no extractable text anyway). A system confirmation ("📎 Attached...") is appended to the live chat on a successful upload so it reads as a real event in the meeting, not a silent background write. Verified live via direct Python execution (bypassing the port-8001 zombie-process issue documented separately): `_subject_note_name` correctly resolved a real meeting's relative path, `save_document`/`list_documents` round-tripped correctly, and the written note's frontmatter/sections matched the `file` Template exactly.

## Entry 019  (Decisions section, 1725 chars)

- **[2026-08-27] Cron jobs and their own gateway are scoped PER-PROFILE in this real Hermes install, not global — a real, non-obvious operational fact, found live recreating the vanished `meeting-capture-recurring` cron job.** `hermes cron list`/`cron status` (no `-p`) only shows/reports the DEFAULT profile's own jobs and gateway process; `hermes -p meeting-prep-agent cron list`/`cron status` shows a COMPLETELY DIFFERENT job set and its OWN separate gateway state — the default profile's gateway being alive (confirmed running, PID 5016) says nothing about whether `meeting-prep-agent`'s own gateway is. The original `meeting-capture-recurring` job (built earlier this session under `meeting-prep-agent`, "confirmed working") had disappeared entirely by 2026-08-27 with no diagnosis possible from here (likely whatever wiped/reset that profile's own Hermes state also took its gateway registration and cron job together) — recreated via `hermes -p meeting-prep-agent cron create "every 30m" "..." --skill meeting-capture --name meeting-capture-recurring --deliver local`, then `meeting-prep-agent`'s own gateway separately started (`hermes -p meeting-prep-agent gateway run`, backgrounded, matching how the default profile's own gateway is run — not installed as a persistent Windows service, operator's own explicit choice) since creating the job alone does NOT start its profile's gateway. **Generalizes: before assuming a cron job "isn't firing" is a job-configuration problem, check that SPECIFIC profile's own `cron status` for its gateway state — a job can be perfectly configured and still never fire if its own profile's gateway isn't running, and the DEFAULT profile's gateway state is not evidence either way.**

## Entry 020  (Decisions section, 1815 chars)

- **[2026-08-27] `.second-brain` (Second Brain's own app-database folder) is now a real, independently-configurable setting (`settings.second_brain_data_path`), not hard-derived from `vault_path`.** Reason: operator, designing the System settings page — "Decouple it into a real setting... Thats the Purpose," then "Move the real folder on save" (not just repoint) once told ~10-12 files hard-derive it. Fixed by adding the new config field (defaults to `<vault>/.second-brain` for a fresh install — zero behavior change until the operator explicitly relocates it) and switching every real path-computation site (`vault_writer.py`'s 24 occurrences, `vault_migration.py`, `upload_storage.py`, `email_staging.py`, `registry/loader.py`, `vault_manager.py`'s Templates lookup via its one backend caller in `documents.py`) onto it. `vault_manager.py` is a physically-copied shared file (same source also lives standalone in every Hermes Skill's own `scripts/` folder, per the earlier `[[project_vault_redesign...]]`-adjacent "prepare here, apply to a real Hermes install" convention) — its Templates subpath constant was deliberately diverged FROM the canonical Hermes-Provisioning source in this backend's copy only, with an explicit comment warning a future re-copy not to silently clobber it. **Real, disclosed, unresolved consequence**: Hermes Skills have no knowledge of this backend setting and will keep reading/writing Templates at `<vault>/.second-brain/` regardless of where the operator relocates the App Database Folder from the UI — only this backend's own state follows the move. Verified live: real move logic tested against temp directories (nested content preserved, old path fully gone, refuses non-empty destination, refuses self-nesting, no-ops on unchanged path) rather than against real vault data.

## Entry 027  (Decisions section, 1304 chars)

- **[2026-08-27] `hermes_client.py`'s Hermes session-token mechanism is fixed now — set `HERMES_DASHBOARD_SESSION_TOKEN` in Hermes' own root `.env` and the matching `HERMES_API_KEY` in this app's own `.env`, both to the same real generated value.** Root cause: the token itself is always minted server-side (`hermes_cli/web_server.py`'s `_resolve_session_token()`), but headless `hermes serve` stopped embedding it in HTML (`GET /` 404s there now, `hermes dashboard` still serves it) — this app's ONLY token-fetch path was scraping that HTML, so it silently broke for every live chat feature whenever Hermes ran headless (the documented, intended way to run it). `_resolve_session_token()` reads `HERMES_DASHBOARD_SESSION_TOKEN` from the environment first if set, generating a random one only as a fallback — setting it to a fixed value plus this backend's own already-coded `settings.hermes_api_key` manual-override path solves it permanently, no more scraping ever. **Generalizes: if a Cockpit chat reply ever comes back "couldn't be reached," check `hermes serve` is actually running (not just Hermes generally) before assuming the bug is in this app's own code — and if the token mechanism ever needs revisiting, the fixed-env-var approach here is the confirmed-working one, not `hermes dashboard`.**

## Entry 035  (Decisions section, 2146 chars)

- **[2026-08-31 Decision]** Gave Opportunity the same Log/Captures split Customer/Partner already has — operator: "the Opp has one file it should have Capture and log as well." Real `Template.json` change (`root.children`: `log`/`captures`, `children_index_section: "Log & Captures"`, replacing the old inline `## Log`/`## Files` sections with `## Summary`/`## Actions`/`## Related`/`## Log & Captures`). This needed one genuinely new, generic engine capability `create_companies_partners`'s own children never needed: **`modify_section()`'s new `child_suffix` parameter** — once the root note is resolved (or freshly created) the normal way, the section write itself redirects to `<root-stem>-<child_suffix>.md` instead of the root, refusing with a real error if that child isn't actually declared/created. This is what makes Log/Captures usable for real — Customer/Partner's own log/captures are still just blank placeholders nobody writes structured content into yet, but Opportunity's Job 2 (Update) actively appends dated Log entries and Captures notes, so the addressing had to be real, not just the file's existence. Job 2's payload gained a `child_suffix` field (`"log"`/`"captures"`, omitted for Actions/Related/Summary); `SKILL.md` bumped to 0.4.0. **Migrated the operator's own 13 real existing Opportunities** (operator chose migrate over leave-as-is): extracted each one's real `## Log`/`## Files` section content (only ONE, "ADNOC AVS→Azure Native Landing Zone Amendment", had real Log content — a 2-entry diary about a contract amendment; the other 12 were empty), wrote it into new `-log.md`/`-captures.md` children, stripped the two old sections from each root, and added the auto-populated `## Log & Captures` index — verified afterward: zero remaining `## Log`/`## Files` headers anywhere, all 13 have exactly one `-log.md` + one `-captures.md` + a populated index section. Verified via 3 new regression tests (44/44 total) plus a live scratch-vault create+append-twice run before touching real data. Deployed the updated engine to all 49 real active `vault_manager.py` locations plus the refreshed `SKILL.md` to `opp-manager`.

## Entry 038  (Decisions section, 876 chars)

- **[2026-08-31 Decision]** `artifact_import.py` (`REQ-SB-85-US-03-T05`) derives a freshly-imported Skill's `name`/`description` by parsing its own real, bundled `SKILL.md` YAML frontmatter (same `_FRONTMATTER_BLOCK` regex + `yaml.safe_load` convention `app/hermes/skills.py::_skill_from_md` already uses for a deployed Hermes-side skill) — a scope-internal judgement call, not a manifest-shape change: `.sbf`'s own frozen manifest shape (`ADR-013`) never carries Skill metadata (name/description live only in the target's own Registry `Tools/<tool>/Skills/<id>/Skill.json`, which an export never includes), so there was no other real source for these two `SkillManager.create()`-required params on a no-conflict import. Confirmed live: a scratch Skill's own `name: zz-import-test-skill` frontmatter line round-tripped correctly into the freshly-created Skill's real metadata.

## Entry 040  (Decisions section, 2550 chars)

- **[2026-09-01 Decision]** Built `vault_manager.py`'s real, generic dynamic (unbounded) child-note primitive (`REQ-SB-87-US-01-T01`, `ADR-017`) — the engine's own module docstring had already anticipated this exact case since 2026-08-30 ("fixed/dynamic `children` (OKF, Thread's messages/) are a real, later addition to this same shape, not built here"). A `root.children` entry gains an optional `"growth": "fixed" | "dynamic"` field (default `"fixed"`, zero behavior change to any already-`Done` template — confirmed live via `test_growth_defaults_to_fixed_and_existing_fixed_children_are_unaffected`). A DYNAMIC entry is identified within `root.children` by its own `"name"` field (a NEW key, distinct from a fixed entry's `"suffix"`, since a dynamic child has no filename-suffix concept) and declares `"folder"`/`"identity_fields"`/optional `"frontmatter_defaults"`/`"sections"`. New verb `create_dynamic_child(vault_path, template, root_id, child_name, identity, frontmatter=None, sections=None)` (CLI: `create-child`): resolves the root via an UNSCOPED `find_by_id` (never fabricates it — the caller isn't expected to already know the root's own `note_name` folder), lists real files under `<root's-own-folder>/<declared-folder>/*.md` and idempotently matches on the declared `identity_fields` natural key before ever writing a new one, reusing `_unique_dated_path`'s own never-overwrite discipline for the new filename (falls back to `"-".join(identity.values())` as the slug source when no caller-supplied `frontmatter["title"]` exists — a dynamic child has no root-level `title` concept). `create()`'s own two fixed-children loops (the `children_index_lines` comprehension and the atomic sibling-write loop) both now skip any `growth: "dynamic"` entry — it is never written at root-creation time. Verified live via a real scratch-vault CLI session (not just pytest): two separate root notes, each carrying its OWN `parent`-resolved (one auto-created, one top-level-optional) container folder, each grew its own `messages/`-equivalent dynamic-child folder independently — the SAME `identity` values under two different roots produced two genuinely separate files, never a collision, and a repeated `create-child` call with identical identity on the same root was a real, confirmed no-op (same path back, exactly one file on disk, original section content untouched) — proving composition with the engine's existing `parent`/fixed-`children` mechanisms with zero regression. 50/50 `test_vault_manager.py` cases pass (44 pre-existing + 6 new).

## Entry 041  (Decisions section, 2437 chars)

- **[2026-09-01 Decision]** Gave `vault_manager.py`'s section-access model real per-caller granularity (`REQ-SB-87-US-01-T02`, `ADR-017`) — replaces `email-thread-capture`'s own hand-rolled `vault_lib.py::_CALLER_ALLOW_LISTS` Python dict with real `Template.json` data, per `ADR-017`'s own explicit rejection of a hardcoded-per-caller-Python alternative. A section entry in `root.sections` gains an optional `"allowed_callers": [str, ...]` list alongside its existing `"access"` key (new helper `_section_allowed_callers`). `create()`/`modify_section()` gain a matching optional `caller: str | None = None` parameter (CLI: `--caller`), threaded to `_require_machine_write(template, section, caller)` at its own two existing call sites (`create()`'s `on_existing_title="update_section"` early-return section-write loop; `modify_section()`'s own update-path write) — after the existing binary `machine_write` check, a section whose own `allowed_callers` is present and non-empty additionally refuses any `caller` not on that list with a real `VaultManagerError` naming both the section and the refused caller. A section with NO `allowed_callers` key stays open to any `machine_write` caller regardless of `caller` — zero behavior change for every already-`Done` template (Customer, Partner, Opportunity, Meeting, meeting-series, Note, File, azure-kb-doc, compass-kb-doc, research-kb-doc), none of which declare it. Verified live via a real scratch-vault CLI session mirroring the real Thread shape (`## Related` → `link_person_to_thread` only, `## Files` → `capture_attachments`/`capture_file_link` only, `## Summary` → `apply_thread_review` only, `## Personal Notes` → `human_only` with zero exception): every allowed-caller write landed, every disallowed-caller write was refused with the section+caller both named in the error and left the section's prior content genuinely untouched (never a partial/silent write), and this composed cleanly in the SAME session with `T01`'s dynamic-child primitive (`create-child` on the same root, idempotent 2nd call) and the engine's existing fixed-child/`parent.on_missing="auto_create"`/link-back mechanisms — zero regression. Also confirmed live on a plain no-`allowed_callers` template with `--caller` omitted from the CLI entirely: both `create` (with initial section content) and `modify-section` succeeded exactly as before. 52/52 `test_vault_manager.py` cases pass (50 pre-existing + 2 new).

## Entry 045  (Decisions section, 2401 chars)

- **[2026-09-01 Decision]** Migrated `ingest_email.py`'s Thread resolve/create and `last_message_at` advances-only stamp onto `vault_manager.py` (`REQ-SB-87-US-02-T01`) — **the Thread's own real, stable `id` is its `conversation_id` itself**, passed straight through as `note_id=conversation_id` on the `create()` call (never a minted `uuid4()`) — a freshly-created Thread has a real, meaningful external key from birth, so there is nothing to backfill later. This is a deliberately DIFFERENT choice than `REQ-SB-87-US-04-T01`'s own real finding the same day (`apply_thread_review.py`'s migration hit real, pre-existing Thread notes with NO `id` field at all and minted a random `uuid4()` on first touch, backfilling it) — confirmed non-conflicting: that backfill only fires when `id` is genuinely absent, so any Thread this migrated `ingest_email.py` creates (real, non-empty `id=conversation_id` from birth) is left untouched by `apply_thread_review.py`'s own backfill, and `id=conversation_id` remains the real, permanent identity for it going forward. `title=conversation_id` too (not the subject) — this is what keeps the real on-disk folder/filename keyed exactly as `vault_lib.py`'s own `thread_directory_paths` always has (`Work/Threads/<conversation_id-slug>/<conversation_id-slug>.md`); `rename_thread.py` (`T02`) is still the only thing that ever relabels it to a human `"<date> <subject>"` form. RawMessage creation stays on `vault_lib.create_raw_message_note`, UNCHANGED — see the Constraint entry directly below for why, and `ESC-061`/`REVIEW-QUEUE.md` for the open decision. Live-verified against a real ~100-email scratch-vault sample (pulled via the real, unmodified `list_recent_emails.py` against live Outlook): 51 distinct Threads/100 messages created identically in a migrated run vs. a same-sample pre-migration baseline run, zero frontmatter value mismatches on any shared real field, all 100 RawMessage notes and all 156 Person notes byte-for-byte identical between the two runs, and idempotent re-ingestion (including an earlier-`received` regression check) confirmed live. The migrated Thread note carries 4 additive-only new frontmatter keys vs. today's real shape (`id`, `title`, `created`, `classification`) plus an always-present empty `## Files` header — all already-disclosed, accepted additive normalizations (`REQ-SB-87-US-01-T05`'s own precedent), not value changes.

## Entry 047  (Decisions section, 2971 chars)

- **[2026-09-01 Decision]** `ESC-061` resolved same-day as a direct fix (operator-directed, no new story/task — same precedent as `BUG-041`'s "Just fix Straight no need for story"): `create_dynamic_child()` (canonical `Hermes-Provisioning/shared/vault_manager.py`) gained an additive `body: str | None` parameter, mutually exclusive with `sections` (a real `VaultManagerError` if both given) — when given, the dynamic child's body is written exactly as-is (`"\n" + body`, reproducing `vault_lib.py::_write_frontmatter_note`'s own real `frontmatter + "\n\n" + body` separator shape byte-for-byte), never a synthetic `## Header`-wrapped document. The original `sections`-based write path is completely unchanged for any other dynamic-child consumer (none of today's real ones declare `sections` either way besides this new mode's own test fixtures). **Chose this over BOTH options `ESC-061` originally named** — not a `"Body"` section on the Thread template's `messages` child (would have been a real, unwanted regression to the live, already-correct RawMessage shape every already-captured note has), and not left unresolved. All 84 real deployed `vault_manager.py` copies resynced (11 repo + 73 live Hermes profile, SHA-256-confirmed byte-identical post-resync) — `src/backend/app/vault/vault_manager.py` deliberately EXCLUDED from this resync (a different, actively-imported FastAPI-app module sharing this file's name/docstring lineage, explicitly called out as "a different thing" by its own sibling module's docstring, not part of the Hermes-Provisioning "canonical + physically-copied Skill scripts" deployment model). `ingest_email.py`'s RawMessage creation then migrated onto the new mode — idempotency is now the engine's own real `(conversation_id, message_id)` identity-field match, not a bespoke filename-existence pre-check, so `ensure_bare_person_note` is called unconditionally on every ingest now (safe — its own contract is already documented idempotent, insert-a-missing-key-only). **One real, disclosed divergence, NOT silently absorbed:** `create_dynamic_child()`'s own generic filename mechanism names a dynamic child from TODAY's real ingestion date + a wall-clock collision suffix, not `vault_lib`'s bespoke received-date + message-id-hash-suffix scheme — changes the on-disk chronological sort order of RawMessage notes within a Thread's `messages/` folder once this migration eventually cuts over to the live vault (content/idempotency both unaffected either way, confirmed live). Flagged in `REVIEW-QUEUE.md` for human spot-check ahead of `REQ-SB-87-US-02-T05`'s own future real-vault cutover. `REQ-SB-87-US-02-T01` re-verified live against a fresh real ~100-email scratch-vault sample and marked `Done`: 100/100 RawMessage bodies byte-for-byte identical to a true pre-migration baseline, 156/156 Person notes identical, 0 real frontmatter mismatches beyond already-accepted additive keys, idempotency/advances-only `last_message_at` reconfirmed.

## Entry 055  (Patterns section, 1077 chars)

- **Email/threaded-source template shape (`email-thread`, 2026-09-04):** a threaded source is ONE note per thread, not one per message, keyed on the provider's own conversation id as frontmatter `id`. `modify_section(note_id=<conversation id>, section="Messages", mode="append")` is "create if not exist, else append" in a single call, so a re-run of the same message set appends to the same file instead of duplicating -- verified live (2nd call on the same id returned `created: False, updated: True` on the same path). `note_own_folder: true` gives the thread its own folder so attachments sit beside the note. Sections split by real ownership using the vault's established access vocabulary (`machine_write` / `human_only` / `public`): `Summary` + `Messages` machine-written, `Actions` + `Personal Notes` `human_only` -- the engine REFUSES an automated write to those (`_require_machine_write` raises), so human ownership is structural, not a prompt instruction anyone can drift from. Confirmed live. The same shape fits any threaded source (chat, tickets), not just email.

## Entry 065  (Patterns section, 1463 chars)

- **`.sbd` real-vault-data-export orchestrator shape (`ADR-016`, `REQ-SB-86-US-02-T02`):** a thin `business/logic/` orchestrator (`vault_export.py::build_export`) composes an already-built resolver (`T01`'s `resolve_embedded_attachments`) with a pure zip writer (`sbd_archive.py::write_archive(output_path, members: dict[str, str])`) — the writer takes ONLY a flat `{archive_member_path: source_file_path}` mapping and makes zero business decisions (no manifest, unlike `sbf_archive.py`'s always-writes-`manifest.json` shape); every decision (which files belong, hierarchy-vs-flat member-path computation, flat-collision disambiguation) happens in the orchestrator, one layer up. Flat-collision disambiguation groups the whole export set (selection + resolved attachments) by basename FIRST, then renames every entry in any group with count > 1 by prefixing `Path(relative_path).parent.name` (real, on-disk casing, no forced-lowercase) — falls back to the literal string `"root"` when the colliding file sits directly at the vault root (empty parent name). The router route mirrors `REQ-SB-85-US-02-T04`'s own `/commit` shape exactly: build to a scratch temp path, `background_tasks.add_task(os.remove, scratch_path)`, stream via `FileResponse` with an ISO-8601-UTC-timestamped filename. First concrete instance: `POST /vault/export-data/export`. Apply the same shape to any future "compose a resolved file set into one real, member-path-computed archive" export.

## Entry 068  (Patterns section, 990 chars)

- **Structurally strip a cloned Hermes profile down to a bounded, zero-tool relay target — don't rely on SOUL.md prose alone (`REQ-SB-87-US-03-T02`):** `hermes profile create <name> --clone` inherits the source profile's FULL skill set and `platform_toolsets.cli` list — for a profile meant to be reached only via one-shot `chat -q` with everything it needs already embedded in the question text (no tool use required or wanted), delete the cloned profile's entire `skills/` directory (`hermes skills opt-out --remove --yes` alone only strips *bundled* skills, not *local*/project-specific ones also copied by `--clone` — those need direct filesystem removal) AND edit the profile's own `config.yaml` → `platform_toolsets.cli: []`. This makes "no vault-write capability, no tool-calling loop" a real, structural property (confirmed live via `sessions export`'s own `tool_call_count: 0`), not just something the prompt asks for. Apply to any future bounded, non-agentic relay-target profile.

## Entry 071  (Constraints section, 1352 chars)

- **Settled vocabulary, do not drift: `Tool -> Skills -> Actions`. A TOOL is a group of SKILLS managing a set of ACTIONS** (operator, 2026-09-04). Three different things in this codebase have been called "tool", which is why every discussion of it drifted: (1) **Tool** as defined here -- the Skill grouping, `<data>/data/Tools/<tool>/Skills/<skill>/`, live and load-bearing (`SkillManager` resolves Skill metadata through it); (2) **the Hermes toolset** -- Hermes' own built-in capabilities (`terminal`, `file`, `memory`, `browser`), a different vocabulary owned by a different system, which is what `GET /tools` and an Agent's `tools` field actually mean; (3) a dead `Tool -> Category -> Action` MCP mounting layer (`data_access/system/tools/`), an abandoned trial, REMOVED 2026-09-04 (it had still been wired into `main.py`, registering nothing, long after a 2026-08-27 entry described the backend's own MCP tool server as already retired -- the retirement was partial and the mounting code outlived it). The TAXONOMY of (3) was right and survives as (1) -- MCP as the *delivery mechanism* is what failed, and Skills became Hermes-native scripts instead (`ADR-002`). That is exactly why the dead code reads as though it were the live model. Always write "the Hermes toolset" for (2), never "Tools". Full framing: `Documentation/Framework/README.md`.

## Entry 075  (Constraints section, 1264 chars)

- **A `Pipeline`'s own `PipelineStep.id` (e.g. "fetch-meetings") carries NO real Agent/Skill linkage — it exists purely to drive the Job Tree visualization. The ONE real path from a Pipeline to the Skill that implements it is `cron_job_id`/`cron_profile_id` → the live Hermes cron job (`HermesCron.list_cron_jobs`/`get_cron_job`) → that job's own `skill` field.** Found live 2026-09-01 (`BUG-041`): `artifact_dependency_resolver.py`'s Pipeline branch had been trying to resolve each step id as an Agent id, which silently failed 100% of the time (steps are never real Agent ids) — no Pipeline export ever included its real Skill until fixed. The cron job's own `skill` field is inconsistent in shape across real jobs — confirmed live: bare slug for some (`meeting-capture-recurring` → `"meeting-capture"`), Hermes' raw `"<category>/<slug>"` form for others (`azure-kb-refresh` → `"knowledge-base/azure-kb-writer"`), and empty for real jobs that aren't Skill-backed at all (git-sync, index rebuilds) — always normalize via the last `/`-segment and treat empty/missing as "no Skill", the same convention `Agent.skill_ids` already established. `PipelineManager.get_implementing_skill_id()` is the composed helper; use it rather than re-deriving this lookup elsewhere.

## Entry 078  (Constraints section, 1055 chars)

- **This vault's real, current OKF Customer/Partner directory convention does NOT literally repeat `index.md`/`log.md`/`captures.md` filenames across folders as `REQ-SB-54`'s original naming implied — the real, live convention (confirmed 2026-09-01 while verifying `REQ-SB-86-US-02-T02`) is `<Name>.md`/`<Name>-log.md`/`<Name>-captures.md`, each already disambiguated by the entity's own name.** A flat-extraction filename collision therefore does NOT occur naturally between two different real Customer/Partner folders today — it can still occur between any two files that happen to share a bare filename for other reasons (e.g. two attachments both named `diagram.svg` in different folders), so the collision-disambiguation logic itself is still required and correct, just not exercised by the specific "two Customers' own `index.md`" scenario the story's own illustrative text assumed. Worth checking directly against real content before assuming a specific "natural" collision case exists, rather than trusting an older story's own naming description.

## Entry 084  (Constraints section, 1308 chars)

- **Hermes' real local API (`hermes serve`) runs on `127.0.0.1:9119` and authenticates via a
  page-embedded session token, NOT port 8642 with a bearer API key.** Found live, 2026-08-20:
  the original `hermes_client.py` was built from research/documentation that didn't match the
  actually-installed Hermes version (`hermes-agent==0.20.4`) at all — `hermes gateway` is the
  messaging/cron daemon only (confirmed via its own startup banner: "Messaging platforms + cron
  scheduler"), never a REST API. The real backend is `hermes serve`, and its real auth is
  `GET /`'s own HTML embedding `window.__HERMES_SESSION_TOKEN__ = "..."` — a per-install access
  token (`auth_required: false`, not a login credential), sent back as `x-hermes-session-token`
  on every subsequent call. Confirmed by inspecting a live browser dashboard session's own
  network requests (monkey-patched `window.fetch`) and independently replaying the captured
  header via a plain `curl` outside the browser — both succeeded against real endpoints
  (`/api/status`, `/api/sessions`, `/api/sessions/stats`, `/api/config`, `/api/profiles/active`)
  returning real data. Before trusting ANY documented Hermes API shape again, verify it live
  against a real running instance first — the official docs/README did not match reality here.

## Entry 086  (Constraints section, 704 chars)

- **A Hermes gateway/CLI session's system prompt (`SOUL.md`/`AGENTS.md`) is injected once at session creation, never re-read mid-conversation — and `session_reset.mode` defaults to `none`, so a WhatsApp thread can run indefinitely on a stale prompt.** Editing `SOUL.md` has zero effect on an already-open chat; the operator (or the agent, via the gateway) must send `/new` (or `/reset`) in that specific thread to start a session that actually picks up the change. Found live 2026-08-22: a 2-day-old WhatsApp session kept asking hallucinated, non-schema questions ("Expected Close Date", "which System") for `track-opportunities` a full session after the real SOUL.md/Skill fix had already been deployed.

## Entry 089  (Constraints section, 1513 chars)

- **Hermes' embedded two-way chat is NOT a REST call — it's a newline-delimited JSON-RPC 2.0 protocol carried over one WebSocket (`/api/ws`), the same one the desktop app and the in-browser Chat tab both drive.** Auth reuses the identical per-install session token `hermes_client.py` already scrapes from `GET /`'s HTML, passed as `?token=` on the WS handshake (a browser WS upgrade can't carry a custom header, so this is the one endpoint that accepts the credential as a query param instead of `x-hermes-session-token`). Real, live-confirmed shape: `session.create` (optionally with `profile: <name>` to scope to a non-default specialist; omit entirely for Primary — `"default"` is the launch identity itself, not a real `profiles/default` dir) returns `session_id`; `prompt.submit {session_id, text}` acks fast (`{"status":"streaming"}`) while the real reply streams back as `event` frames on the same socket (`message.delta` chunks, `message.complete` with the FULL final text, plus `thinking.delta`/`status.update`/`session.info` in between); approval/clarify prompts arrive the same way (`approval.request`/`clarify.request`) and are answered with `approval.respond`/`clarify.respond` carrying the event's own `request_id`. Confirmed by hand-driving the real protocol against the live gateway (got a genuine model reply back) BEFORE writing `hermes_ws_client.py`'s FastAPI integration — the same "verify live before trusting a documented/assumed shape" discipline as the earlier `hermes serve` REST rebuild.

## Entry 091  (Constraints section, 1263 chars)

- **[2026-08-23] `vault_indexing.py`'s own index has zero disk persistence (ADR-024's own explicit, accepted tradeoff — a plain module-level Python dict, rebuilt wholesale only when something calls `rebuild_index()`) — this became a real, user-visible bug once Hermes' own capture pipelines moved fully outside this backend process (2026-08-21 self-hosting rewrite), removing the last thing that ever called it automatically.** Every backend restart (a frequent, normal dev-mode event, `--reload` included) silently emptied the whole Browse & Search page back to "Nothing indexed yet" until a human remembered to call `POST /vault-index/rebuild` by hand — reported live as "shows the wrong notes then when I refresh it is showing no notes." Fixed by rebuilding eagerly in `main.py`'s own lifespan on every start (non-blocking background task, `asyncio.to_thread` since `rebuild_index()` itself is synchronous/blocking). General lesson: a module-level in-memory cache with no automatic refresh trigger is a real bug waiting to happen the moment its LAST refresh trigger gets removed elsewhere in the system — worth re-auditing every "no persistence, rebuilt on trigger X" design (ADR-024-style) whenever trigger X's own owning process/pipeline moves or is removed.

## Entry 095  (Constraints section, 1364 chars)

- **[2026-08-23] `section_registry.py`'s own persisted `assignments` store (ADR-014) is NOT what controls which Section an individual Hermes-mirrored agent (Primary/opp-manager/notes-manager/files-manager/azure-calculator) shows up in on the Agents Map — `agents_map_adapter.py` never reads it for those ids.** That store is scoped to the now-fully-retired Second-Brain-native agent model (`agent_registry.py`'s own 2026-08-22 emptying); calling `section_registry.set_agent_section("opp-manager", ...)` would silently persist an assignment nothing ever reads. The REAL mechanism (added 2026-08-23) is `agents_map_adapter.py`'s own `_AGENT_SECTION: dict[str, str]` — a plain agent_id → Section-name override, same shape/reasoning as the pre-existing `_AGENT_TYPE` map right above it, with an id absent from the dict falling through to the shared `_SECTION_NAME` default ("Data Gatherer"). Pipelines (Company Discovery/Meeting Builder/Threads Builder) are a THIRD, separate mechanism — each has its own real `section` field directly in its `system/pipelines/<id>.json` file, read via `pipeline.section`, never touching `_AGENT_SECTION` or `section_registry` at all. Three real, independent placement mechanisms for what looks like one concept on the map — check which kind of node (individual Hermes agent vs. Pipeline) before assuming which one governs its Section.

## Entry 096  (Constraints section, 1542 chars)

- **[2026-08-24] `Work/Emails/` no longer exists — email capture moved wholesale to the Threads Builder pipeline (2026-08-21) and now lives under `Work/Threads/<thread>/`, with THREE note kinds mixed in that one folder tree: `type: "Thread"` (one distilled conversation summary, the real "email list item" equivalent), `type: "RawMessage"` (one per individual message, has its own real `sender`/`subject`/`received`), and `type: "File"` (attachments).** Any code still checking for a flat `Work/Emails/` folder or treating `list_notes_in_kind_folder("Emails")` as meaningful is reading a dead data source (found live: `my_day.py`, silently returning `[]` for as long as this has been true, since an empty list looks identical to "genuinely nothing captured this week"). A Thread note has no single `sender` of its own (it's a conversation, not a message) — `my_day.py`'s fix resolves a best-effort "most recent real sender" by cross-referencing RawMessage notes sharing the same `conversation_id`, matching Thread's own `## Actions`/`## Related` sections being genuinely per-conversation, not per-message. General lesson: whenever a capture pipeline gets rebuilt (Threads Builder replacing flat Emails, or any future rebuild), grep for every OTHER reader of the OLD folder/shape before considering that pipeline's own rebuild complete — a silently-empty downstream projection is much easier to miss than an outright 404, and can persist for a long time unnoticed (this one dated back to the 2026-08-21 rebuild, three days before being found).

## Entry 107  (Constraints section, 700 chars)

- **[2026-08-25] `hermes profile describe <profile> --text "..."` is the real, correct CLI mechanism for setting a profile's own `profile.yaml` (`description` + `description_auto: false`) — confirmed live: `hermes profile create --clone` does NOT generate a `profile.yaml` at all (absent from the freshly-cloned directory), it is created only on the first `profile describe --text`/`--auto` call.** Every already-Done specialist profile (`notes-manager`, etc.) has one with the exact same two-key shape (`description`, `description_auto: false`), confirming this is the live, standing convention for authoring a new profile's description going forward, not something to hand-write as a raw YAML file.

## Entry 112  (Constraints section, 1071 chars)

- **[2026-08-25] A Hermes profile created via `hermes profile create <name> --clone` inherits config/credentials (`.env`, `config.yaml`'s `platforms.whatsapp.home_channel`, etc.) from `default`, but NOT an already-paired platform connection — WhatsApp specifically needs its own separate, real, human-interactive QR-code pairing (`hermes -p <name> whatsapp`) per profile before that profile's own gateway can stay running.** Found live, `REQ-SB-82-US-05-T02`: `meeting-prep-agent`'s gateway exited immediately on start with `WhatsApp enabled but not paired` even though its `.env`/`config.yaml` carried the exact same real WhatsApp settings as `default`. A cron job can be perfectly created and correctly scheduled (`hermes cron list`/`cron status` both report it as `active`) and still never fire unattended until this separate, real-world action is completed by the operator — confirmed this install's own standing pattern (every other specialist profile's gateway is also "not running" by default, per `hermes gateway list`), not something this task's own build broke.

## Entry 115  (Constraints section, 1313 chars)

- **[2026-08-27] Agent/Section ownership split, stated by the operator for a not-yet-built `AgentManager`: "agents assigned to section" is a function ON `AgentManager` (`agent_manager.assign_to_section(...)`-shaped, exact shape deferred — operator: "we will get to the how later"), never something `SectionManager` computes or owns internally.** This directly explains (but does not yet fix) the empty-`agent_ids` bug found earlier the same day (`GET /sections` always returning `agent_ids: []` because `list_sections`/now `SectionManager.get_all()` only ever read the dead pre-Hermes `assignments` dict, never the Registry's real `AgentConfig.section_id`) — the real fix is NOT "make SectionManager read the Registry directly instead," it's "let `AgentManager` own the agent→section relationship once it exists, and have cross-manager business logic (operator: "cross managers work is the business logic") compose `SectionManager` + `AgentManager` when something needs the joined view," matching the same boundary `SectionManager` itself just established for Sections. Don't have `SectionManager` reach into Agent/Registry data to "fix" `agent_ids` before `AgentManager` exists — that would just recreate the exact cross-entity-logic-living-in-the-wrong-Manager problem this whole entity split is meant to avoid.

## Entry 116  (Constraints section, 2115 chars)

- **[2026-08-28] `AgentManager` built, both read and write sides — the settled `Agent` JSON schema, and the real design behind it, worth preserving in full since it took a whole discussion pass to ground in real Hermes/Registry data rather than guess.** Real Hermes fields (`app/hermes/profiles.py`'s `HermesAgent`): `name`(real only for "default"; else = raw profile id unless the Registry has a friendlier one), `description` (`profile.yaml`), `model`/`reasoning_effort` (`config.yaml`), `skill_ids` (the real `skills/` tree). Second-Brain-only fields, confirmed by the operator during design: `type` is now `worker | producer | expert | hub` — **"hub" is new**, the agent that represents a Section on the map (the "create Section ⇒ create its hub Agent" rule the operator stated, not yet wired — `AgentManager.create` can make a `type: "hub"` agent today, but nothing calls it automatically from `SectionManager.create` yet, deliberately deferred same as the Agent/Section ownership split above). `working_mode` (`autonomous | human_in_loop`) is REAL now, not fake — operator: "does the agent need a human in the loop to operate", and confirmed it should reuse Hermes' OWN real `approval.request`/`approval.respond` mechanism (already wired at the transport level in `app/hermes/chat_session.py`, already surfaced as read-only text in `chat_sessions.py:format_approval_request`) rather than a new Second-Brain-native approval queue reviving the deleted `pending_approval_registry.py` — but there is still no real per-agent store for it (stays a constant `"autonomous"` today) and the actual "hold a request_id open so a human can answer later instead of Hermes auto-timing-out" piece is unbuilt. `managed_by` (`"hermes"` today) replaces `provider_id`/`AgentConfig.provider_id` — operator: "all agents are hermes so provider_id might be useless." `prompt`+`guardrails`+`scope` (`{folders, tags}`) all compose into ONE real Hermes `SOUL.md` write — operator: "Both go into SOUL.md" — confirmed `prompt` is genuinely the full real SOUL.md text (`HermesProfiles.read_soul`), not the short `profile.yaml` description.

## Entry 121  (Constraints section, 2007 chars)

- **[2026-08-28] Every real agent's Registry data is now ONE file, `Agent.json`, not a separate `Agent-config.json` + `Agent-visual.json` pair (operator: "we should have one Json file describing the Agent").** `AgentVisual` (the Registry's own dataclass) is deleted entirely — icon/color are just two more fields directly on `AgentConfig` now (nothing real was lost: grepped first and confirmed the Registry's own `Agent.visual` field, like `Agent.soul`, had ZERO real readers anywhere — both were purely loader fail-loud-check artifacts, never actually consumed downstream). `loader.py`'s `_load_one_agent` reads the merged file; `AgentManager._write_registry_agent` writes it; `agent_visual_registry.py`'s own Registry-mirror write (`_write_registry_agent_visual`) now does a real read-modify-write on `Agent.json`'s `icon`/`color` keys instead of writing a second file. **Real, live production data migrated, not just new code written for new agents going forward**: all 21 real agent folders (20 under Sections/, 1 Background/default) had their `Agent-config.json`+`Agent-visual.json` merged into `Agent.json` and the two old files deleted, via a one-off inline script (not a committed file) — confirmed the Registry genuinely fails to boot beforehand against the old format (fail-loud working as designed), then confirmed a clean boot afterward with real data intact (`adnoc-expert` still resolves `icon: "apartment"`, correctly merged from its own real Agent-visual.json). `tests/test_registry_loader.py`'s own `_seed_minimal_valid_tree` fixture updated to the new one-file shape. Verified live end-to-end: boot-status `ready`, `GET /agents` unchanged (28 total), a real `PATCH` (color edit, then reverted) round-tripped correctly through the merged file, and the Agents Map rendered unchanged in the browser ("6 sections · 36 agents mapped", ADNOC's own icon intact). Skill's own equivalent `Skill.json`/`Skill-visual.json` split was NOT touched — out of scope, the operator named Agent specifically.

## Entry 125  (Constraints section, 2839 chars)

- **[2026-08-28] `Pipeline` gained a real link to the Hermes cron job behind it — `cron_job_id`/`cron_profile_id`, composed live into `cron_enabled`/`cron_schedule`/`cron_last_run_at`/`cron_next_run_at`/`cron_last_status` via `HermesCron` — closing the exact same class of gap the Agent schema pass found: real structured data (which cron job, its schedule, whether it's enabled) was sitting as unstructured PROSE inside `Pipeline.description` ("...the real Hermes cron job behind this is X (every Y), running the Z Skill"), invisible to anything but a human reading it.** Checking this against real data surfaced a genuine, deeper gap in `HermesCron` itself: `list_cron_jobs`/`get_cron_job` were hardcoded to only ever read the shared default/root profile's own `cron/jobs.json` — confirmed live that `meeting-builder`'s own claimed cron job, `meeting-capture-recurring`, does NOT exist there at all (grepped the real file, confirmed absent), which could have been wrongly read as "the pipeline's description is stale/wrong." Instead of assuming that, searched every real profile's own `cron/jobs.json` and found it genuinely exists — just under the `meeting-prep-agent` profile's own cron (job id `70894beaabab`, schedule `every 30m`, skill `meeting-capture`), a completely different, unrelated profile from what its NAME would suggest. **Fixed properly, not worked around**: `HermesCron` (`app/hermes/cron.py`) gained an optional `profile_id` param threaded through all 4 real methods (`list_cron_jobs`/`get_cron_job`/`list_cron_executions`/`get_execution_detail`) plus the private path helpers, defaulting to the exact same default-profile-only behavior when omitted (confirmed backward compatible -- the pre-existing caller, `cron_status.py`, never passes it and needed zero changes). Confirmed live that cron output reports and `agent.log` are ALSO genuinely per-profile (`profiles/<name>/cron/output/`, `profiles/<name>/logs/agent.log` both real, both exist) before scoping those paths too, not just jobs.json. `cron_job_id` stores the job's own stable `name` field (e.g. "meeting-capture-recurring"), never Hermes' own opaque per-job hash `id` (e.g. `70894beaabab`) -- the hash is Hermes' own internal identifier, `name` is the stable, human-chosen one already embedded in every pipeline's own prose description. All 3 real pipeline JSON files updated with the confirmed-real linkage (`company-discovery`→`new-company-discovery`@default, `threads-builder`→`email-delta-capture`@default, `meeting-builder`→`meeting-capture-recurring`@`meeting-prep-agent`). Verified live: all 3 pipelines correctly compose real, current cron status (`meeting-builder` correctly resolves cross-profile, showing `enabled: true` from the `meeting-prep-agent` profile's own cron), `GET /agents` unchanged (28 total), Agents Map unchanged in the browser.

## Entry 126  (Constraints section, 1786 chars)

- **[2026-08-28] `SkillManager`/`ToolManager` built — Skill's real data is genuinely split across TWO stores, by design, confirmed against real disk/live-Hermes data before building.** CONTENT (`SKILL.md`+`scripts/`) lives in the checked-in `Hermes-Provisioning/skills/<category>/<slug>/` "template repo" (`data_access/skills.py`) — operator: "we do a copy for all Skills inside our System." METADATA (name/description/tool grouping/`deployed_to`/`mutates`/`origin`) lives in the pre-existing Registry `Tools/<tool>/Skills/<slug>/{Skill.json, Skill-visual.json}` tree (`data_access/tools.py`) — the SAME tree `RegistryLoader`/`agents_map_adapter.py` already read for the Agents Map Skills panel, so `SkillManager` writes keep that panel current for free, no new store needed. `Skill.id` is the plain slug, NOT `"<category>/<slug>"` despite the original scaffold's own comment — confirmed live against real `Agent.json` files (`skill_ids: ["capture-files", "summarize-and-tag-files"]`, no category prefix) and the existing `Skill.json`'s own `"id"` field before committing to the format; a real live data contract beats an unimplemented scaffold comment. `Tool` is a generic, user-creatable grouping (operator: "the tools are generic... those are created tools using the generic template") — `compass`/`outlook`/`vault`/`web` are just the Tools that already existed, not a fixed set; `ToolManager.delete()` refuses while real Skills are still grouped under it (reassign via `SkillManager.update(tool_id=...)` first). **`deploy_to`/deployment is confirmed 1:many from our side, 1:1 on Hermes' side** (operator: "Our store is 1:many hermes is 1:1") — one Skill's `deployed_to` list fans out to N individual `app.hermes.skills.HermesSkills.create/update/delete` calls, one per real profile.

## Entry 128  (Constraints section, 2459 chars)

- **[2026-08-28] `SkillManager.sync_from_hermes()` — the operator's requested cron-driven drift-catcher (a live profile has ~80 bundled third-party hub skills under unrelated categories like `apple`/`github`/`creative`; the sweep above only covers OUR 17) — hit two real bugs on its first live run, both found by inspecting actual written output, not by trusting a clean-looking return value.** (1) `app.hermes.skills.HermesSkill.id` is `"<category>/<slug>"`, NOT the plain slug our own `Skill.id` uses — comparing/storing it directly (instead of the dataclass's own separate `.slug` field) silently mis-detected every real, already-known skill as "new" on the first pass, writing bogus SELF-NESTED folders (`Hermes-Provisioning/skills/company-review/company-review/...`, `Tools/jarvis/Skills/company-review/create-companies-partners/...`) across all 7 categories — caught only by directly inspecting the real written files after the run, not by the function's own reported `imported`/`reconciled` counts, which looked plausible. (2) Even after fixing the id, an unassigned skill's metadata write (`_write_meta`'s own `tool_id or "jarvis"` fallback) wrote real `Skill.json` files under `Tools/jarvis/Skills/` without ever creating `Tools/jarvis/Tool.json` itself — since `find_tool_id_for_skill`/`ToolManager.get_all()` both require a real `Tool.json` to even look inside a Tool's own `Skills/` folder, this made the written data permanently invisible to every future read, silently "fixed" nothing on repeat runs. Fixed by moving the `_ensure_jarvis_tool()` call into `_write_meta` itself (any write that resolves to the jarvis fallback now guarantees the Tool exists first), not just the one call site that happened to trigger it during design. **General lesson, worth repeating**: after ANY write-heavy sync/reconcile method's first live run, inspect the real files it wrote directly (`find`/`Get-ChildItem`, not just the function's own return value) before trusting it — a plausible-looking summary count is not proof the write targets were correct. Verified clean on the corrected re-run: `imported: []` (matches the sweep's own already-complete 17), `reconciled: 56` deployed_to entries exactly matching the manual sweep's own tabulated profile counts, disposable-data CRUD lifecycle (create → update+reassign-tool → delete, Tool included) smoke-tested and confirmed zero leftover artifacts, `app.main` imports clean, AST dangling-reference check clean.

## Entry 136  (Constraints section, 2476 chars)

- **[2026-08-29 Decision] Real architecture pivot from a whiteboard discussion (not yet built, recorded here so it isn't lost): a Section IS its Hub Agent, not a graphical grouping represented by one.** Operator's own correction: "Sections is Representation... Sections are actually an Agent we call it a hub it's not a Graphical Representation any more." The Hub's real purpose (operator-confirmed, "Yes, that matches"): a department head, not a fallback-when-nothing-matched afterthought — for any question, it makes one of 3 real decisions: (1) answer itself from its own broad domain scope (already real — `customer-hub`/`industry-hub`/etc. already carry every real tag in their domain), (2) delegate DOWN to one of its own section's specific experts (the existing parent→child `regenerate_specialists_section` mechanism), or (3) redirect SIDEWAYS to another Hub whose domain actually covers it (Hub-to-Hub peer routing — NOT built yet, would need a new peer-list-shaped block, since every relay mechanism built so far is parent→child, never peer→peer). Current real Hub descriptions ("Fallback conversational agent for the X Section") are the OLD framing and are now stale/wrong against this decision — not yet rewritten. Latency/reliability concern about multi-hop relay (raised after a real live test where `industry-expert` skipped its own correctly-wired relay text twice) was explicitly reframed by the operator: synchronous chat-testing latency doesn't matter once the real target is autonomous async triggers (an email arrives, the agent chain runs in the background, a human only sees the final output) — the reliability-of-firing concern stands independently, but the latency objection doesn't. Also surfaced, not yet resolved: whether "Section has a list of registered Agents" should be a real STORED list (risks reintroducing the exact two-sources-of-truth shape that caused the duplicate-Agent.json bug fixed earlier the same day) or stay derived-at-read-time as `agent_ids` already is today. **Tools/"Powers" identified as a real, missing 3rd Agent axis, confirmed technically real (not speculative)**: `hermes -p <profile> tools list/enable/disable <toolset>` is a genuine, durable, per-profile mechanism, verified live — distinct from both Section (identity) and vault-tag Scope (data access, has zero effect on tool availability). Not yet wired into `AgentManager`/the API at all (no field, no endpoint) — flagged as real future work, not built this pass.

## Entry 138  (Constraints section, 4037 chars)

- **[2026-08-28] Section creation now creates its own Hub Agent — a genuine Manager-to-Manager call, a deliberate exception to the "compose at the caller" rule established for `agent_ids`.** Operator's own reasoning for the exception: hub creation is an intrinsic part of what "creating a Section" MEANS (echoing the much earlier "create section is not complete till we finish the agent as well"), not an aggregate query across many agents — those stay composed at the router. `SectionManager.create()` now: creates the Section as before, then calls `AgentManager().create(f"{section_id}-hub", ..., type="hub", section_id=<the new section's own id>)`, then links the returned hub id back as the Section's own `fallback_agent_id`. Implemented with a LAZY import inside the method body, not at module level — `AgentManager` already imports `SectionManager` at module level (to resolve a not-yet-migrated agent's own fallback section), so a module-level import the other way would be a genuine circular import; confirmed this resolves cleanly (app imports fine, no runtime recursion, since `AgentManager.create()` only calls `SectionManager.get_by_id()`, never back into `SectionManager.create()`). **Second real behavior added**: `AgentManager.create()` now defaults `depends_on` to `[<the Section's own Hub agent id>]` whenever a new agent is created with no explicit `depends_on` and isn't itself the Hub (`type != "hub"`, guarding against a Hub depending on itself) — every new agent is a specialist of its Section's Hub by default now, matching the real relay-chain shape already hand-built for compass-expert's/azure-expert's own children. `is_background_agent` stayed a plain separate bool, NOT folded into a fake "Background" Section value — operator: "wasn't a fan of making Background a Section" — `section_id` is still always required and still used for display/fallback resolution the same way Primary/`default` already works, unchanged. **Retroactive fix applied to real, live data**: the 4 pre-existing hand-built hub agents (`customer-hub`/`industry-hub`/`technology-hub`/`sales-hub`) were all `type: worker` and all mis-placed under `data-gatherer` (the same "not yet migrated" fallback every unmigrated agent gets) — fixed via `AgentManager.update(type="hub", section_id=<real section>)` for all 4, confirmed live on the real `Agent.json` files and via a fresh Registry boot (`data-gatherer` dropped from 7→3 agents, the other 4 sections each gained their own correctly-placed, correctly-typed hub). **Real operational gotcha hit AGAIN during verification, worth remembering as its own standing lesson** (this is now the second time this exact class of bug has bitten a scratch-agent verification script): a test script that deletes an agent then immediately calls `SectionManager.delete()` on its section, WITHOUT an explicit `registry_loader.boot()` in between, can have the section-delete silently BLOCKED — the delete's own Registry-based check reads the stale in-memory cache, still sees the just-deleted agent as occupying that section. Always re-boot between a real mutation and any check/mutation that depends on seeing its result, never assume same-process calls see each other's writes. Also confirmed a real, separate pre-existing gap while cleaning up from this: `SectionManager.delete()` removes the Section from its own state list but never deletes the physical Registry folder mirror (`data/Sections/<id>/`, containing `Section.json`) -- left as a known, minor gap, not fixed in this pass (out of scope, not what was asked). Verified live end-to-end: a full scratch section→hub→specialist chain (create, verify hub type/section/empty depends_on, verify specialist auto-depends-on-hub, delete, confirm zero leftover artifacts in both Hermes and the Registry), plus the real production fix (`GET /sections` now shows `customer: 4, librarian: 3, industry: 1, technology: 11, data-gatherer: 3, sales: 3` agents, summing to the same 28 total), and the Agents Map renders `customer-hub` as type HUB in the browser.

## Entry 139  (Constraints section, 2374 chars)

- **[2026-08-29] `tools` wired as a real, 3rd `Agent` field (alongside `section_id`/identity and `scope`/vault data access) — closes the gap identified in the same day's Hub-purpose discussion.** Real, per-profile Hermes CLI state (`hermes [-p <profile>] tools list/enable/disable`), no `--json` mode exists (confirmed against its own `--help`) so `HermesCLI.list_tools()` parses the real, stable text format (`"  ✓ enabled  web  ..."`) via regex — verified live against `adnoc-expert`'s real 26-toolset state before trusting it. `HermesCLI` gained its first-ever per-profile targeting (`_profile_args`, a `-p <profile>` prefix) — every other method in that class implicitly only ever operated on the default profile. **Deliberate perf-conscious design**: reading `tools` is a real subprocess call per agent, not a cheap in-memory read like `scope`/`skill_ids` — populating it inside `_to_agent()` unconditionally would slow `GET /agents` (which composes EVERY real profile) by one subprocess spawn per agent. Fixed by adding `include_tools: bool = False` to `_to_agent()`: `get_by_id()` (one agent) passes `True`, `get_all()`'s own bulk composition leaves it `False` (`Agent.tools` stays `[]` on a list-composed Agent — confirmed live, both behaviors verified separately). `AgentManager.update(tools=[...])`/`create(..., tools=[...])` both take a full REPLACE list (same declarative "this is exactly what should be enabled" convention as `scope`'s own folders/tags) — diffed against the real current enabled set (`update` reuses `current.tools`, already fetched via its own `get_by_id` call; `create` fetches the freshly-cloned profile's inherited set once) and applies only the delta via `enable_tools`/`disable_tools`. Wired end-to-end: `AgentCreateBody`/`AgentUpdateBody` both gained `tools: list[str] | None`, `to_detail_dict` exposes it. **Verified fully live, Manager layer AND the real HTTP route** (not just unit-level): a disposable agent created via direct Python inherited its clone source's full toolset, then `update(tools=[...])` correctly replaced it down to an exact requested set, confirmed idempotent on a second identical call; a SEPARATE disposable agent driven through the real `POST /agents` (tools set at creation) → `PATCH /agents/{id}` (tools changed) HTTP round trip both confirmed correct; both scratch profiles confirmed fully deleted afterward.

## Entry 143  (Constraints section, 1874 chars)

- **[2026-08-31 Constraint] `Agent.skill_ids` (`AgentManager._to_agent`, `[skill.id for skill in hermes_agent.skills]`) is Hermes' own raw `"<category>/<slug>"` skill id form — confirmed live against every one of the 40 real Agents in this deployment (e.g. `compass-expert.skill_ids == ['knowledge-base/compass-kb-writer']`), never the bare slug `SkillManager`/`data_access.skills` key everything by (`SkillManager().get_by_id('knowledge-base/compass-kb-writer')` is `None`; `SkillManager().get_by_id('compass-kb-writer')` resolves).** Any future caller that looks up one of an Agent's own `skill_ids` entries against `SkillManager` must first take the id's own last `/`-separated segment (the same "HermesSkill.id is category/slug; ours is the plain slug" convention `SkillManager.sync_from_hermes()` already established, `2026-08-28`) — otherwise the lookup silently resolves to nothing for every real Agent that has any Skill at all. Found live building `artifact_dependency_resolver.py` (`REQ-SB-85-US-02-T02`, `ADR-013`)'s Agent→Skill traversal. Also confirmed live the same pass: no real Pipeline in this deployment (`company-discovery`/`meeting-builder`/`threads-builder`) has `steps[].id` values that are real Agent ids (every real Pipeline's own steps are pipeline-internal data-capture stage identifiers with their own separate step-ordering `depends_on`, not Agent references), and no real Agent currently has a non-empty `depends_on` (all 40 are `[]`) — the compass-pricing-expert/compass-expert "specialist" relay is realized via hand-written `SOUL.md` prose (`AgentManager.regenerate_specialists_section`), not the structured field. A future caller relying on either "a real Pipeline step names a real Agent" or "a real Agent has a real `depends_on` entry" as a positive example should verify against current data first, not assume either shape already exists.

## Entry 145  (Constraints section, 1427 chars)

- **[2026-09-01 Pattern] `thread/Template.json` (`REQ-SB-87-US-01-T05`) is the first `vault_manager.py` template to mirror Customer/Partner's own `own_folder + plain_filename + plain_folder` shape (a durable, name-keyed entity with no date anywhere in its path) for an entity that is NOT parent/child-linked — Thread has no `"parent"` declaration at all, unlike every other template using this same folder shape so far.** `on_existing_title: "always_new"` was deliberately chosen over Customer/Partner's own `"error"` — a duplicate real Subject line across two genuinely different, unrelated conversations is common and correct (never a mistake to refuse, and never a case where merging writes into the pre-existing same-titled note would be safe), unlike a duplicate Customer/Opportunity NAME, which really is a mistake. Paired with `on_missing: "error"` (diverging from every other real `always_new` template in this vault, which all pair with `on_missing: "create"`) — deliberately, since none of Thread's own real callers (`link_person_to_thread`/`capture_attachments`/`capture_file_link`/`apply_thread_review`) should ever be able to fabricate a Thread via a bare section write; only `ingest_email.py`'s own explicit, deliberate `create()` call (its first-sight branch, migrated separately in `US-02`) may create one. Disclosed as a scope-internal judgement call in the task's own Implementation Log, not silently assumed.

## Entry 147  (Constraints section, 1786 chars)

- **[2026-09-02 Pattern] Noise-definition artifact (`ADR-018`, `REQ-SB-87-US-03-T01`) lives entirely OUTSIDE this repo, at `.second-brain/data/EmailCapture/noise_definition.json` under the real VAULT (`<OPERATOR_VAULT_OLD>`), a real sibling to the vault's own `.second-brain/data/Templates/` tree — never a repo-tracked file, never a Skill-`scripts/`-folder file.** Its derivation script (`Hermes-Provisioning/skills/vault-rebuild/email-thread-capture/scripts/derive_noise_definition.py`) relays ONE one-shot `hermes [-p PROFILE] chat -Q --query-file <scratch-file>` question to a real Hermes profile and persists the response verbatim — the definition's own CONTENT is real LLM reasoning, this script's own code is limited to prompt-building/relay/parse/persist. Confirmed live: the default/root profile (no `-p` flag at all) is a legitimate, sufficient target for this DERIVATION call — a dedicated classifier profile is only needed for the separate, later per-email classify-or-skip relay (`REQ-SB-87-US-03-T02`), not for building the definition itself. Two live derivation runs (one against the 5 real operator-locked seed subjects, one against 3 different real subjects) confirmed the artifact genuinely changes per-sample (not a frozen file) and consistently frames the category as "anything automated/broadcast," never narrowing to literal meeting invites (already filtered upstream in `outlook_lib.py` via `MessageClass`/`_MEETING_MESSAGE_CLASS_PREFIX`, confirmed directly in that file before this task started). A failed relay/parse leaves any previously-persisted artifact untouched by design (`derive()` only writes after a successful parse) — apply this same "never clobber on failure" shape to any future out-of-band, on-demand artifact-regeneration script in this project.

## Entry 157  (Constraints section, 3547 chars)

- **[2026-09-02 Pattern] Reusable, parameterized retrofit/backfill CLI for `email-thread-capture` (`retrofit_capture.py`) — a THIN SIBLING subprocess-orchestrator, deliberately not a refactor of the already-Done, already-verified `run_full_capture.py`/`run_delta_capture.py` per-email loops.** Built to close the real gap the Constraint entry above (`REQ-SB-87-US-02-T05` post-hoc finding) documents: a retrofit driver that calls `ingest_email.py` DIRECTLY, one message at a time, never gets `rename_thread.py`/`link_person_to_thread.py`/`capture_attachments.py` for free — those only fire as part of the orchestrators' own per-email loop. `retrofit_capture.py --vault-path P [--limit N]` (default 100) pulls the last N real Inbox+Sent items via one bounded `list_recent_emails.py --limit N` call (no `--before` paging needed — `list_recent_mail`'s own merge/sort-newest-first/trim-to-limit already returns exactly the last N in one call) and, for each message IN THE ORDER RETURNED (newest-first, never re-sorted — matches both the orchestrators' own per-page dispatch shape and `REQ-SB-87-US-02-T05`'s own real 100-message retrofit precedent), chains ingest → link-person (if a sender_email is present) → rename (ALWAYS, idempotent no-op once already renamed — the exact step a direct-`ingest_email.py`-only driver skips) → attachments (only when the message has attachments and ingest returned a real `message_path`), via the SAME `run_script()`-style subprocess pattern `run_full_capture.py` already established, its own dedicated scratch-payload subfolder (`second_brain_retrofit_cli`, distinct from the orchestrators' own `second_brain_capture_cli`, to avoid any filename collision if run alongside a live cron tick). Chosen over factoring the per-email loop out of the two orchestrators into a shared helper both would import — the smaller, lower-risk change (zero risk of regressing either orchestrator's own live cron-facing behavior); revisit as a real shared helper only if a third caller of this exact sequence ever appears. Prints one final JSON summary (`total_processed`/`already_existing_topped_up`/`genuinely_new_captured`/`skipped_as_noise`/`errors`), mirroring `run_full_capture.py`'s own summary shape. Adds no new dedup logic — relies entirely on `ingest_email.py`'s own already-established `(conversation_id, message_id)` natural-key idempotency, so safe to re-run against the same vault/limit repeatedly. Proven against a scratch vault (`--limit 15`, real live Outlook data into a scratch vault copy: 15 processed, 8 genuinely new, 2 topped up, 5 correctly skipped as noise, 0 errors — all 8 newly-created Threads already correctly renamed to their readable `<date> <subject>` form, zero raw-hex-named stragglers, the exact failure mode this script exists to prevent) before the real `--limit 100` run against the live vault. Apply this same "thin sibling CLI reusing the existing per-step scripts via subprocess, one bounded pull, preserve the source's own ordering, no new dedup logic" shape to any future one-off/reusable retrofit tool needed for this or another already-Done recurring pipeline. Real `--limit 100` live-vault proof-run result (2026-09-02, once `email-delta-capture`/`job5` were both confirmed idle): 100 processed, 97 already-existing topped up, 1 genuinely new captured, 2 correctly skipped as noise, 0 errors — the one new real Thread already correctly renamed/classified/person-linked, real Thread count 259→260 (exactly +1), zero raw-hex-named folders found in a full `Work/Threads/` sweep afterward.

## Entry 160  (Constraints section, 1625 chars)

- **[2026-09-02 Pattern] `build_entities_report.py` (entity-domain-extraction Step 1) now refuses to overwrite an `Entities.md` that already has real curated entries, unless `--force` is passed.** Root cause of today's whole "Entities.md came back to the vault" episode: this script's own always-full-rewrite design (documented, deliberate for a genuine first seed) has no guard against being re-run against an already-curated file — exactly what happened when a stale deployed copy (wrong output path) ran, and the same design flaw would have destroyed real curation even on the CORRECT path if re-run there. Fix: a cheap `### ` heading count against the target file (not a real parse — this script only ever needs "is there already real curation here", not the entries themselves) refuses with a clear JSON error + entry count when non-zero, unless `--force`. Verified live: refused against the real vault (48 entries, exit 1, file untouched); succeeded cleanly against a genuinely fresh scratch vault (0 entries); re-running against that still-entry-less result stayed safe. Resynced to all 36 real deployed copies. **This is also the real "seed a new person's vault without risk" mechanism going forward** — running this script against a brand-new person's own empty vault (e.g. onboarding a new deployment) works exactly as before; the guard only ever fires once real curation exists, which is precisely when a full-rewrite would be destructive. Apply the same "cheap existence/non-emptiness check, `--force` to override" shape to any other one-time/first-seed script whose own design is a full, unconditional overwrite.

## Entry 163  (Constraints section, 4832 chars)

- **[2026-09-03 Pattern] `tools/hermes_backup.py`/`hermes_restore.py` — a real, tested `.sbb` (zip) backup/restore pair for the Hermes-side structural content (Agents/Profiles' own SOUL.md+skills, Cron, Skills) plus the Second Brain app's own matching data, built for the operator's own laptop migration.** Deliberately excludes Hermes instance-level `config.yaml` (get fresh via `hermes setup` on the new machine) and every real secret (`.env`, `auth.json`, `channel_directory.json`, `shared/nous_auth.*`) — operator, 2026-09-03: "Secrets is what i meant with settings." Two real, distinct path-rewrite targets, not one: the vault path AND the full Hermes-home path (which embeds the Windows username — `SKILL.md` files hardcode this in their own example terminal commands, e.g. `C:\Users\<user>\AppData\Local\hermes\skills\...\vault_manager.py`) — confirmed live via a real content scan of the actual backup archive that these two are the ONLY hardcoded-path families present anywhere in what's bundled (nothing else needed). Also excludes `__pycache__`/`.pyc` (Python-version-specific, regenerable, found live bloating the archive with cpython-311/314 mismatches). **Two real bugs found and fixed live while building/testing, not before:** (1) `_disabled-skills-on-primary` was listed both explicitly AND caught by the generic `_disabled-skills*`-prefix scan, double-adding every file inside it (`zipfile`'s own "Duplicate name" warning surfaced this). (2) A genuinely serious one: `cron/jobs.json` was originally bundled INSIDE the generic per-profile overlay tree (`hermes/default/cron/jobs.json`) — restore's plain file-copy overlay step ran BEFORE the dedicated merge-by-id cron logic, so it silently clobbered the target's own real `cron/jobs.json` wholesale before the "never silently overwrite a colliding job id" guarantee ever got a chance to apply. Reproduced live on scratch (a real, differently-content job at the same id was overwritten), then fixed by bundling cron/jobs.json as a fully separate top-level archive member (`cron/jobs.json`, not nested under any profile path) that ONLY the dedicated merge function ever touches — re-verified live afterward: a real target-side job with the same id as a backed-up one now survives completely untouched, correctly reported as `skipped_existing`. **Restore validates everything (manifest present/parseable, every bundled JSON file parses, target Hermes home is already real/configured, target vault path exists, `hermes` CLI is on PATH) BEFORE any real write** — operator, 2026-09-03: "should say error in restore instead of Destroying my self" — a validation failure returns a clean `{"status": "refused", "problems": [...]}` and touches nothing; any exception after that point (e.g. a real `hermes profile create` failure) is caught at the top level too, always a clean JSON error, never a raw traceback. The one piece that can't be tested in true isolation is `hermes profile create <name> --clone` for a genuinely new profile — it shells out to the real, global `hermes` CLI regardless of any `--hermes-home` override, so proving it needed a real (disposable, cleaned-up-after) profile against the actual machine, not a scratch sandbox. `default` is never profile-created by restore, only overlaid — the target's own `default` is assumed already real per Deployment.md Sections 1-2 (its own real secrets configured there first). **A third real bug, found live from the operator's own "why is it 400 MB" question on the very first real backup of this actual machine**: a profile's own deployed `skills/` tree was being bundled wholesale, but that tree ALSO carries every STOCK skill bundled with Hermes itself (e.g. `research/research-paper-writing`'s own full set of academic conference LaTeX templates — real, non-trivial size) — not unique data, already available fresh on any new Hermes install via its own skill management. A second real contributor found in the same investigation: `.hub/index-cache/hermes-index.json`, a 39.6 MB Hermes-internal search-index cache, byte-for-byte near-identical across all 40 real profiles. Fixed: Skills are now bundled per profile ONLY for this project's own canonical skill ids (derived live from `Hermes-Provisioning/skills/<category>/<name>/` in this repo, never hand-listed, so it can't go stale as skills are added) — a non-canonical/stock skill folder in a profile's own tree is never bundled at all; `.hub/` excluded the same way as `__pycache__`. Real result against the actual current machine: 400 MB / 18,112 files → **7.1 MB / 1,490 files**. Regression-verified on scratch afterward that a real canonical skill (`vault-rebuild/email-thread-capture`, `librarian/person-lookup`) still bundles and restores correctly, and a deliberately-planted non-canonical skill folder is correctly absent from the restored target.

## Entry 164  (Constraints section, 2817 chars)

- **[2026-09-03 Pattern] `Settings > Backup & Restore` (`SettingsBackupPage.tsx`, `POST /backup/export`/`/backup/restore`) — a real, live-tested thin UI/API layer over `tools/hermes_backup.py`/`hermes_restore.py`, not a re-implementation.** `app/business/logic/hermes_backup.py` shells out to the exact same tested scripts via `subprocess` (same "trust an already-correct external executable" shape `HermesCLI` already establishes for the real `hermes` CLI) — zero risk of the API surface subtly diverging from what was already scratch-and-live-verified at the CLI level. Export streams the real scratch `.sbb` back as a download (same `FileResponse` + `BackgroundTasks.add_task(os.remove, ...)` convention `artifact_export.py`'s own `/commit` route established); restore accepts a real multipart file upload (same `UploadFile`/scratch-path convention `artifacts_router.py`'s own `/import/commit` established) and returns the script's own real structured JSON verbatim, or raises `HTTPException(400, detail=<that same structured JSON>)` on refusal/failure — the frontend unwraps FastAPI's own `{"detail": ...}` envelope to get back exactly what the script itself reported. **Live-verified end-to-end, not just code-reviewed:** Export — clicked the real button in the real browser, confirmed a real `200 OK` on `POST /backup/export` in the network log. Restore — curl's own `-F "file=@path;type=..."` syntax silently mis-parses in this shell (`curl: (26) Failed to open/read local data from file/application`; fixed by dropping the inline `;type=`), then a real multipart POST against a tiny scratch backup succeeded fully: created a real new disposable profile via the real `hermes profile create` call, correctly refused to touch the real vault's own already-populated `.second-brain/data`/`pipelines` (`160`/`13` existing files, exactly the guard from the CLI-level testing working through the HTTP path too), and rewrote both real paths correctly in the response. **Because ANY restore through this real endpoint always overlays "default" too** (there's no way to exclude it from a manifest), and the test backup's own `default` carried placeholder SOUL.md content, the REAL current machine's own real `SOUL.md` was saved (md5 hash recorded) before the test and restored byte-for-byte afterward (verified via matching md5) — same "verify a scripted write against real, unbacked-up data" discipline as this session's own cross-project memory. The disposable test profile was deleted afterward (`hermes profile delete <name> -y`). **Generalizes: any future real-endpoint test of this specific restore path must budget for `default` always being touched — save its real SOUL.md first, or build a source backup that deliberately carries default's OWN current real content (a true no-op) rather than placeholder text.**

## Entry 166  (Constraints section, 3128 chars)

- **[2026-09-03 Pattern] `hermes_backup.py`/`hermes_restore.py` switched from literal old-path→new-path string rewriting to PLACEHOLDER TOKENS (`@@SECOND_BRAIN_VAULT_PATH@@`/`@@SECOND_BRAIN_HERMES_HOME@@`/`@@SECOND_BRAIN_DATA_PATH@@`) — a real, live bug in the rewrite design, found testing this same day's OWN third-rewrite-target fix, is what motivated it.** The bug: `second_brain_data_path` is a real, longer string that literally CONTAINS the vault path as its own prefix whenever it's vault-relative (the common case, `<vault>\.second-brain`) — rewriting the vault path FIRST silently consumed that shared prefix, leaving the data-path rewrite nothing left to match; restore then silently fell back to the WRONG (vault-relative) location instead of the real, resolved target, reproduced live on a deliberate scratch test with a relocated target data path. Operator, on hearing the root cause: "<OPERATOR_VAULT_OLD> should be a place holder and pass it on Restore and same for the other Path" — a categorically more robust design than trying to get old→new substring-overlap ordering right by hand. **New design:** `hermes_backup.py` substitutes the real vault/hermes-home/data-path values for their fixed placeholder tokens in every bundled TEXT file's own content (`.md`/`.json`/`.yaml`/`.yml`/`.py`/`.txt`) at the moment each file is written into the zip (`_write_member`, threaded through a small `_PathContext` rather than a global, since every `_add_*` call site needs the same three real values) — binary content (profile tarballs, curator blobs) is written unchanged, substitution never attempted on bytes that aren't real text. `hermes_restore.py` substitutes the placeholders back for ITS OWN real, resolved target paths after extraction, with NO dependency on the manifest's own `source_*` values at all (those stay in the manifest purely as informational metadata for a human). Placeholders have no ordering/overlap concern by construction — three fixed, non-colliding tokens, order genuinely doesn't matter, unlike the old design's real prefix-containment trap. **Verified, not assumed:** the exact failure scenario (vault-relative source, relocated target) that broke the old design now produces the correct, relocated target path; re-ran the full backup→restore round trip (lock/pid exclusion, Settings inclusion, index exclusion, per-profile cron, curator backups, real memory) with zero regressions; confirmed against REAL production content (not just synthetic test fixtures) that the exact file which originally surfaced this whole investigation (`vault-index/SKILL.md`'s own `--data-path` example) and real `cron/jobs.json` prompt text both now carry placeholders with zero real-path leakage. **Generalizes: for any future tool that needs to move real, textual, environment-specific content between machines, prefer placeholder substitution over literal-value rewriting whenever any of the substituted values could be a real substring of another — the failure mode is silent (wrong-but-plausible output, not a crash) and easy to miss without a deliberately adversarial test case exactly like this one.**

## Entry 170  (Constraints section, 2135 chars)

- **[2026-09-03 Decision] The Artifacts export/import subsystem (`.sbf` bundles -- `artifact_export.py`/`artifact_import.py`/`sbf_archive.py`, `ADR-013`) gained the exact same placeholder-substitution mechanism as `hermes_backup.py`/`hermes_restore.py`, applied to Skill/Template/Agent-registry/Pipeline TEXT content instead of a whole filesystem tree.** Why: operator, 2026-09-03, planning to extract Skills into a separate "CBO Service" deployment ("Dig Deep in this as will use it to Extract Some Skills and Place it in the CBO Service") -- audited the same portability lens onto this subsystem and found it real: **16 of ~17 real canonical Skills'** own `SKILL.md` bake this machine's literal vault path directly into the exact `terminal(command=...)` block the agent is instructed to run (not just doc prose), and 3 real scripts (`email-thread-capture`/`meeting-capture`) fall back to a hardcoded literal because the env var they check (`SECOND_BRAIN_VAULT_PATH`) is never actually set anywhere on this install. Fixed by substituting real->placeholder on export (`artifact_export.py`, using this machine's own `settings.vault_path`/`.second_brain_data_path`/`.hermes_home_path`) and placeholder->real on import (`artifact_import.py`, using the TARGET machine's own current settings at commit time) across every closure text file (`SKILL.md`, `scripts/**`, `Template.json`, `Agent.json`, `soul.md`, `pipelines/<id>.json`) -- deliberately NOT the nested `agents/<id>/profile.tar.gz` (stays Hermes' own opaque, already-redacted bytes, `ADR-014`'s boundary, untouched, same as `hermes_backup.py` never cracks open a Hermes profile's own internal format). Separately fixed a real data-loss bug found alongside this: `artifact_import.py`'s seed/blank-data writer (`_write_seed_data`, e.g. `Settings/Entities.md`) unconditionally overwrote the target's file with `""` on every import of the owning Skill, even when the target already had real content -- operator: "Just Create it if empty" -- now only writes the blank placeholder when the target file doesn't already exist (`_SEED_DATA_READERS` added, checked before `writer("")` runs).

## Entry 171  (Constraints section, 1830 chars)

- **[2026-09-03 Decision] Agents gained a new, real, editable field `primary_routing_snippet`** (Registry-side metadata, same category as icon/color -- `AgentConfig`/`Agent` dataclasses, settable via `POST /agents`/`PATCH /agents/{id}`, wholesale-included in `.sbf` export/import automatically since Agent.json already travels as a raw dict). Why: operator, 2026-09-03, building the CBO handoff -- discovered "link to Primary" isn't structural at all, it's hand-written prose in Primary's own SOUL.md (`hermes -p notes-manager chat -q ...`), which never traveled with an exported Agent and had no path to travel automatically ("this is big... link the Agent to primary with what you will append, this will make our life easier"). This field is the structured place to store that routing text once, per-agent. **Deliberately never auto-applied to any SOUL.md** -- matches this whole subsystem's "never silent" discipline (same posture as the secret-scan/conflict decisions): `artifact_import.py`'s new `apply_primary_routing_snippet(agent_id, snippet)` is a SEPARATE, explicit, operator-triggered step (`POST /artifacts/import/apply-primary-routing`), idempotent via a per-agent `<!-- BEGIN/END PRIMARY ROUTING: <id> -->` marker pair (same style as `agent_manager.py`'s own `_SPECIALISTS_BEGIN`/`_END`), so re-applying is a safe no-op and a hand-edited snippet already in place is never silently overwritten. Populated for real on `notes-manager`/`files-manager` with the actual routing text pulled from this machine's own Primary SOUL.md. Verified end-to-end with a scratch vault + a throwaway copy of Primary SOUL.md: extract -> placeholder-restore -> apply (appends correctly) -> re-apply (correctly no-ops). **Frontend not yet built** -- no UI button calls the new endpoint yet; for now it's reachable via direct API call only.

## Entry 173  (Constraints section, 777 chars)

- **[2026-09-03 Constraint] `SectionManager.create()` always creates a real Hub Agent alongside a new Section** (`<section>-hub`, deterministic templated prompt, no LLM call) -- confirmed live while tracing what actually happens when an imported Agent's bundle carries no resolvable `section_id` (the normal case: `Agent.json` never carries one at all, confirmed by direct reading). On a fresh target with no "Data Gatherer" section yet, importing ANY agent auto-creates both the section AND its hub agent as a side effect -- 3 real agents land from a 2-agent import, not 2. Not a failure mode, a deliberate existing contract ("a new Section is not complete until its own Hub Agent exists too") -- but worth knowing before assuming an import produced exactly what was selected.

## Entry 174  (Constraints section, 1014 chars)

- **[2026-09-03 Decision] Artifacts import gained a real, explicit section-placement choice, replacing the always-silent "Data Gatherer" fallback.** `preview_import` now returns `available_sections` (every real section on the target); `commit_import` takes an optional `agent_section_decisions: {agent_id: section_id | "__create_new__:<name>"}`. An explicit choice is verified against the target's own real sections and fails loud if it names one that doesn't exist (a real operator mistake, never silently swapped for a fallback); omitting a decision for a given agent keeps the exact prior behavior (falls back to "Data Gatherer", auto-creating it) -- an organizational choice is never allowed to fail the whole import the way an undecided skill/template/agent CONFLICT does. Verified live: existing-section id, invalid id (fails loud), no-decision (unchanged fallback), and create-new (creates a real disposable section + hub agent, confirmed, then cleaned up). **Frontend still not built** -- backend/API only.

## Entry 176  (Constraints section, 1139 chars)

- **[2026-09-03 Decision] Second Brain has a real version number for the first time -- repo-root `VERSION` file (semver), read via `GET /health`'s `version` field, shown in the Sidebar UI (always visible).** Why: operator, 2026-09-03: "I want to have the current version number in the UI" + "If something will break when I pull the new update" -- scoped down (operator's own explicit choice) to version number + semver convention only, no automated breaking-change detection (that would need a defined list of what counts as breaking, which doesn't exist). **Standing process instruction, applies to every future push from here on:** "At the end of every [push] you will include the Version and Change log in the push" -- bump `VERSION` (MAJOR = breaking, read CHANGELOG before assuming a pull just works; MINOR = feature; PATCH = fix) and add a real `CHANGELOG.md` entry under that version's own header, together, as part of the same push. `CHANGELOG.md` retroactively closed everything accumulated before this decision under `## 0.1.0`; a fresh `## [Unreleased]` section holds whatever comes next until it's cut as the next real version.
